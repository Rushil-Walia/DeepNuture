package com.example;
public class PerformanceTester {
    public void performTask() throws InterruptedException {
        // Simulating a task that takes 500 milliseconds to complete
        Thread.sleep(500); 
    }
}